$kind: http-request
name: Set Password
method: POST
url: '{{base_url}}/api/v1/auth/set-password'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "{{invite_token}}",
      "new_password": "newpassword123"
    }
